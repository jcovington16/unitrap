import { getFetcher } from "./client/index.js";
import { hoistApiError } from "./errors.js";
export async function getQuery(config, params, opts = {}) {
    const queryByStatement = getFetcher(config)
        .path("/query")
        .method("get")
        .create();
    const { data } = await queryByStatement(params, opts).catch(hoistApiError);
    switch (params.format) {
        case "table":
            return data;
        default:
            return data;
    }
}
//# sourceMappingURL=query.js.map