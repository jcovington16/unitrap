import { getFetcher } from "./client/index.js";
export async function getHealth(config, opts = {}) {
    const health = getFetcher(config).path("/health").method("get").create();
    const { ok } = await health({}, opts);
    return ok;
}
//# sourceMappingURL=health.js.map